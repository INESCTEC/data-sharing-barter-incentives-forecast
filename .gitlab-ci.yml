stages:
  - test-market

test-market:
  image:
    name: python:3.8
    entrypoint: ["/usr/bin/env"]
  stage: test-market
  script:
    - pip install -r requirements.txt
    - flake8
    - cd src/market
    - pytest --cov
  coverage: '/TOTAL.+ ([0-9]{1,3}%)/'
